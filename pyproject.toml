[project]
name = "claude-agent-sdk-terminal-demo"
version = "0.1.0"
description = "Minimal terminal agent built on the Claude Agent SDK"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk",
]
